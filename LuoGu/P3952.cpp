#include <bits/stdc++.h>
#define endl "\n"
using namespace std;

void solve() {
    //这题模拟一个语法检查，我已经被编译原理暴打半年了，放过我吧
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
